<div class='row'>
	<div class='span8'>
		<h2>Upcoming events</h2>
		<% @events.each do | event| %>
		  <div class="well">
			  <h3><%= event.name %>: <%= event.time.to_date.to_formatted_s(:long) %></h3>
			  <h5><%= link_to "Signup", event.event_url, target: "_blank" %></h5>
			  <%= event.description.html_safe %>
			  <h5>Location</h5>
			  <%= event.venue_name %><br />
			  <%= event.venue_address1 %><br />
			  <%= event.venue_address2 + "<br />".html_safe if event.venue_address2 %>
			  <%= event.venue_address3 + "<br />".html_safe if event.venue_address3 %>
			  <%= event.venue_city %><br />
			  <%= link_to "Map", event.venue_map, target: "_blank" %>
			</div>
		<% end %>
	</div>
</div>